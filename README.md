WebSocket FizzBuzzMoi
===

# Install

    $ dep ensure

# Run

First, start the server:

    $ go run server/main.go

Next, start the client:

    $ go run client/main.go

For more options, use `-h`.

